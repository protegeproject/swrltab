<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
				 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>org.swrlapi</groupId>
  <artifactId>swrltab</artifactId>
  <version>1.0</version>
  <packaging>jar</packaging>
  <name>OWLAPI-based version of SWRLTab</name>

  <properties>

    <owlapi.version>3.5.0</owlapi.version>
    <swrlapi.version>1.0</swrlapi.version>
    <swrlapi.drools.version>1.0</swrlapi.drools.version>
    <log4j.version>1.2.16</log4j.version>
    <junit.version>3.8.1</junit.version>
    <jep.version>2.4.0</jep.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

  </properties>

  <dependencies>

    <dependency>
      <groupId>net.sourceforge.owlapi</groupId>
      <artifactId>owlapi-distribution</artifactId>
      <version>${owlapi.version}</version>
    </dependency>

    <dependency>
      <groupId>org.swrlapi</groupId>
      <artifactId>swrlapi</artifactId>
      <version>${swrlapi.version}</version>
    </dependency>

    <dependency>
      <groupId>org.swrlapi</groupId>
      <artifactId>swrlapi-drools-engine</artifactId>
      <version>${swrlapi.drools.version}</version>
    </dependency>

		<dependency>
			<groupId>log4j</groupId>
			<artifactId>log4j</artifactId>
      <version>${log4j.version}</version>
    </dependency>

    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>

	</dependencies>

  <build>

    <plugins>

      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
				<version>2.3.2</version>
        <configuration>
          <source>1.6</source>
          <target>1.6</target>
        </configuration>
      </plugin>


      <plugin>
				<artifactId>maven-assembly-plugin</artifactId>
				<configuration>
					<archive> 
						<manifest>
							<mainClass>org.swrltab.test.P4SWRLAPIRegressionTester</mainClass>
						</manifest>
					</archive>
					<descriptorRefs>
						<descriptorRef>jar-with-dependencies</descriptorRef>
					</descriptorRefs>
				</configuration>
				<executions>
					<execution>
						<id>make-assembly</id> <!-- this is used for inheritance merges -->
						<phase>package</phase> <!-- bind to the packaging phase -->
						<goals>
              <goal>single</goal>
						</goals>
					</execution>
				</executions>
      </plugin>

    </plugins>

  </build>

</project>
